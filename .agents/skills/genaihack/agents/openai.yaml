interface:
  display_name: "Genaihack"
  short_description: "Repo-specific patterns and workflows for genaihack"
  default_prompt: "Use the genaihack repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true