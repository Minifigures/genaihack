# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Pydantic — unpinned so pip uses pre-built wheels on Python 3.14
pydantic>=2.10
pydantic-settings>=2.6

# AI / Agents
langgraph>=0.2.0
google-generativeai>=0.8.0

# Database (Supabase PostgreSQL)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0

# IBM WatsonX — optional, uses REST API directly (SDK not required)
# ibm-watsonx-ai>=1.3.0  # Uncomment if using SDK instead of REST

# Rate limiting
slowapi>=0.1.9

# Web scraping
firecrawl-py>=1.0.0

# PDF generation
fpdf2>=2.7.0

# HTTP client (for WatsonX REST API)
requests>=2.31.0

# Auth
PyJWT>=2.8.0
cryptography>=41.0.0

# Utilities
structlog>=24.4.0
pyyaml>=6.0.2

# Dev / Testing
pytest>=8.3.0
httpx>=0.27.0
